# Training dependencies (GPU recommended)
torch>=2.2.0
transformers>=4.42.0
datasets>=2.20.0
accelerate>=0.33.0
peft>=0.12.0
trl>=0.9.6
safetensors>=0.4.3

# Optional (only for QLoRA script)
bitsandbytes>=0.43.3
